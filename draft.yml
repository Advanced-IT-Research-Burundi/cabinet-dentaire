models:
  StockMovement:
    stock_id: foreign:stocks,id
    type: string:250
    date: timestamp
    quantity: float
    price: double
    description: string:250 nullable
    status: string:250 nullable
    is_syncronized: boolean nullable


controllers:
  StockMovement:
    resource: web
